# Brand Brain 修复包 V2 部署指南

## 修复内容

| # | 问题 | 修复方案 | 文件 |
|---|------|---------|------|
| 1 | 费用预估虚高¥6700 | 改用真实API成本（~¥0.57） | `src/lib/billing/cost-estimator.ts` |
| 2 | DeepSeek余额读取失败 | 改用官方API: GET /user/balance | `src/app/api/billing/deepseek-balance/route.ts` |
| 3 | 通义万相余额读取失败 | 改用DashScope API: GET /api/v1/account/quota | `src/app/api/billing/dashscope-balance/route.ts` |
| 4 | 余额汇总缺少Token状态 | 新增汇总API含Token到期检测 | `src/app/api/billing/summary/route.ts` |
| 5 | 行业物料不区分 | 新增10行业+通用物料映射 | `src/lib/industry-materials-map.ts` |
| 6 | Logo/IP被限制成小图 | 修正保护规则：不改色但大胆展示 | DecisionLayer patch notes |
| 7 | 三视图识别/裁剪/抠图 | IP资产处理管线（sharp+通义万相抠图API） | `src/lib/ip-asset-pipeline.ts` + `src/app/api/ip/process/route.ts` |

## 部署步骤

```cmd
:: 1. 备份
cd C:\Users\Administrator\Coze\vi-ai-logo-ip-mock
mkdir backup-v2
copy src\lib\billing\cost-estimator.ts backup-v2\
copy src\app\api\billing\deepseek-balance\route.ts backup-v2\
copy src\app\api\billing\dashscope-balance\route.ts backup-v2\
copy src\app\api\billing\summary\route.ts backup-v2\

:: 2. 复制新文件（从解压后的src目录）
xcopy /E /Y src\lib\billing\cost-estimator.ts src\lib\billing\
xcopy /E /Y src\app\api\billing\deepseek-balance\route.ts src\app\api\billing\deepseek-balance\
xcopy /E /Y src\app\api\billing\dashscope-balance\route.ts src\app\api\billing\dashscope-balance\
xcopy /E /Y src\app\api\billing\summary\route.ts src\app\api\billing\summary\
xcopy /E /Y src\lib\industry-materials-map.ts src\lib\
xcopy /E /Y src\lib\ip-asset-pipeline.ts src\lib\
xcopy /E /Y src\app\api\ip\process\route.ts src\app\api\ip\process\

:: 3. 添加环境变量
echo COZE_PAT_TOKEN_EXPIRES=2026-07-04 >> .env.local
echo COZE_PAT_TOKEN=pat_UXpA1VIwns8RJ2yoCaEjf715mn2Lmg9s1jywynLb0iGeNPYFPVIqniwtUwrpiC4l >> .env.local

:: 4. 安装依赖（sharp可能已安装）
npm install sharp

:: 5. 重启开发服务器
npm run dev
```

## DecisionLayer.tsx 手动修改3处

### 修改1：添加 import
```typescript
import { detectIndustry, getIndustryMapping } from "@/lib/industry-materials-map";
```

### 修改2：品牌分析区域添加行业标签
在 step === 1 的 ProfileCard grid 中添加一个卡片：
```tsx
<ProfileCard label="行业适配" value={(() => {
  const industry = detectIndustry(brandAnalysis);
  const mapping = getIndustryMapping(industry);
  return mapping.label + "（物料已适配）";
})()} />
```

### 修改3：受保护资产提示文字
找到 amber-700 的提示段落，将原文替换为：
```
受保护 = 不改色不重绘，但排版上大胆放大、满版展示、居中突出、可裁切做装饰
```

## 验证

1. ✅ 费用预估 ~¥0.57（非¥6700）
2. ✅ DeepSeek/通义万相余额正常显示
3. ✅ 扣子PAT Token到期日 2026-07-04
4. ✅ 行业适配标签
5. ✅ IP三视图：上传→识别→裁剪→抠图
