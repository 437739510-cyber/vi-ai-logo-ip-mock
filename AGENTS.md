# Brand Brain 仓库规则

将本文件复制到以下目录并命名为 `AGENTS.md`：

```text
D:\disk\HermesDisk\bb-clean
```

本文件补充全局 Codex 规则，只适用于 Brand Brain 项目。

## ChatGPT / DeepSeek 模型切换与接班

- Chris 于 2026-08-09 确认：以下规则是 Brand Brain 的长期固定协作方式，不是临时
  建议；后续更换 ChatGPT、DeepSeek 或其他模型时继续沿用，除非 Chris 明确修改。
- 任何进入本仓库的新 Codex 对话，不论接入 ChatGPT、DeepSeek 还是其他模型，
  在调查、修改、评审或开工单前，都必须完整读取：
  `D:\disk\HermesDisk\bb-clean\_bridge\MODEL-SWITCH-HANDOFF.md`。
- 角色由窗口决定，不由模型决定：**主窗永远是大脑，侧窗永远是手**。ChatGPT、
  DeepSeek 只是窗口内可替换的模型，切换模型不能改变窗口职责。
- 主窗只做只读调查、判断、规划、开工单、验收和维护交接文档；除工单、评审、
  接班指针等治理文档外，主窗不得修改生产代码或代替侧窗执行工单。
- 侧窗只按主窗签发的一张明确工单修改白名单内文件、运行测试并写回执；侧窗不得
  自行改变方向、开下一单或验收自己的结果。
- Codex 左侧栏、旧聊天标题和模型记忆不是项目状态真源。接班必须从当前
  `TICKET`、桌面 `汇报\REPORT-*`、`汇报\REVIEW-*`、Git 状态和真实 diff 恢复。
- 模型切换后的第一步只能做只读恢复，并先向 Chris 报告当前模型、当前窗口角色、
  当前工单、恢复状态和下一项安全动作；无法确认自己处于主窗还是侧窗时不得修改代码。
- 执行者完成后写回执，不能自行宣布验收通过；总控依据证据写独立评审，只有
  `REVIEW: PASS` 才代表工单已验收。总控在终态评审后更新接班协议中的当前指针。
- 模型切换不会继承旧聊天里的提交、推送、部署、生产写入、付费调用、读取凭据
  或扩大工单范围的授权；这些操作仍需 Chris 对当前任务明确授权。
- ChatGPT 流量耗尽时，由 Chris 手动切换到 DeepSeek；流量恢复时也由 Chris 手动切回。
  自动调度只负责主窗与侧窗之间的工单、回执和验收接力，不自动修改模型、Provider、
  Codex 配置、认证、会话 JSONL 或索引。
- 通用接班格式固定为 UTF-8 Markdown：`AGENTS.md → MODEL-SWITCH-HANDOFF.md →
  TICKET / REPORT / REVIEW → Git 现场`。原始 Codex JSONL 对话只作历史备查，不直接
  拼接、修改或作为当前项目状态真源。

## 成功经验档案（新会话必读入口）

做 VI 手册 / IP 公仔 / 生图相关任务前，先查阅以下经验档案，避免重蹈覆辙：

- `D:\disk\ComfyUI-模型清单.md`：模型/工作流清单 + 已验证配方。含「IP 公仔
  三视图生成方法（李记案例验证）」：CHAR_BASE 中文角色描述必须写死细节；
  三视角分句（正对镜头 / 右转90度侧面，3/4 可接受 / 背对镜头可见后脑勺）；
  种子用大间隔非连续值；每张间 45s 冷却；PIL 拼合 3192×1239（标题+正/侧/后
  标注）；参数 z_image_turbo_nvfp4、Steps=4、CFG=3.5、Euler、1024×1024；
  场景 Prompt 必须先写角色描述再加场景。
- `D:\disk\prompt-engineering-handbook.md`：提示词工程经验。
- `D:\ComfyUI-backup\gen_3view_sheet.py`：三视图生成+拼合现成脚本（百疗萃
  案例，粉红玫瑰金）。
- `D:\ComfyUI-backup\output\vi_manual_assets\`：JIK0 全套成功资产
  （front/side/back 单视图 + 3view 合拼 + 表情/场景 + 含公仔手册 PPTX），
  作质量与版式参考。
- 桌面 `汇报\REVIEW-* / REPORT-*` 与 `_bridge\GOVERNANCE-LOG.md`：工单级
  经验与决策记录。
- 生图纪律：禁止把「三视图/多视图」写进生图提示词（模型会自绘拼图，必须
  逐张生成→验收→代码合拼）；每张强调「画面中只有一个角色」；LOGO 与中文
  文字由代码后期贴入/叠加，不让 AI 画（AI 中文必乱码）。
- 场景图少文字/无文字原则（Chris 2026-08-12）：宣传/场景图以图说话即可，
  AI 不画文字（必乱码）；无文字或少量代码叠加文字均可接受，不要为凑文字
  （或贴 LOGO）把好构图搞乱。参考：
  `D:\ComfyUI-backup\output\comfyui_zt_00762_.png`（主形象+四小场景构图好，
  去掉 AI 乱码文字即为合格场景图）。
- 按色提取独立 LOGO 方法（2026-08-12 已验证）：从含墙面/背景的图里抠出
  单独 LOGO——① HSV 色键掩码（取 LOGO 主色色域）；② 边缘泛洪填充背景
  （保留 LOGO 轮廓含内部线条）；③ 最大连通域剔除背景杂点；④ 透明底裁出。
  实测产物：`logs/086/logo-extracted-00011-v3.png`（玫红水滴 LOGO，无墙面
  残留、含内部白线、视觉验证通过）。
- 视觉核字：重要核字必须双模型交叉（本地 qwen2.5vl + SenseNova/SiliconFlow
  免费在线通道），不单信一个模型（云端对少文字图会幻觉）。
- 客户上传 Logo 体检与优化（百疗萃灰 logo 案例沉淀）：上传后自动体检
  （色系是否灰/单色/低对比、是否偏离品牌色板、是否含文字、清晰度）；
  不达标自动出优化版（优先程序化重涂保留结构，次选 AI 重绘保留语义，或
  品牌名文字变通）；原图与优化版双版本呈现、选择留痕；不覆盖客户原图；
  核色/结构/核字验证。方向（Chris 2026-08-12 确认）：**LOGO 一律用优化版/
  生成版**——视觉分析客人旧 LOGO + 汇整平台客户信息 → 生成新 LOGO 多方案
  供客人选择（选择留痕）；客人原图只作对比、不出交付页。场景图=「品牌
  视觉氛围示意」（AI 整图生成，不承诺 1:1 还原），场景合成质量单独立清单
  整改（R4 成品问题清单见 _bridge\问题清单-R4成品-2026-08-12.md）。

## 素材真源（2026-08-12 新增，防跨项目误用）

- 专项素材库：`D:\ComfyUI-backup\output\专项素材库\`（参考案例 / 任务资产
  两套目录；README 有铁律）。唯一真源清单：
  `_bridge\资产真源清单.md`（主窗维护，新素材须双模型视觉断言后登记）。
- 关键区分：**JIK0（VI-20260722-JIK0）= 鹿系公仔项目**，鹿角/鹿耳是它
  的正常设计，仅作版式参考，禁止套给萃瑶/人类公仔；**萃瑶（百疗萃 POLP）
  = 人类女神、粉金长发、玫瑰金裙、无角**，正确素材在
  `专项素材库\任务资产\萃瑶-百疗萃POLP-00744锚定\`（00744 锚定 + final-r5）。
- 教训：两次误用（086-R2 鹿角污染、展示页错挂 JIK0 图）同根——把 JIK0
  当成萃瑶旧版本。任何公仔素材进页面/手册前，显式反问「有无角/兽耳/
  动物特征/水印」并双模型交叉（本地 qwen2.5vl + SenseNova/Agnes）。
- 场景图惊喜原则（Chris 2026-08-13）：场景图生成时**允许 AI 把 LOGO
  提示词画进场景**（配「以上为品牌视觉氛围示意」），AI 画的 LOGO 有时
  比客人现有 LOGO 更好看；不要为了精确贴客户 LOGO 而强制“空白底板+
  代码贴”。「品牌视觉氛围示意」的本质是**让平台发挥创新**——AI 画
  LOGO/构图自由发挥，不受客户旧资产约束；中文品牌字仍由代码贴或省略
  （防乱码），LOGO 图形可让 AI 入景。创新地基=平台 AI（DeepSeek）汇整
  客人填写信息后形成的最佳方案，生图在方案框架内发挥，不是无依据乱创新。
  **AI 图总定义=品牌视觉氛围示意**；场景图小字无所谓（不核小字），只
  保证品牌名 4 字大字清晰正确（四字保证、五字抽卡；4 字+现代扁平大字
  可靠，印章/篆书/小字必错）；参考样板=order_VI-20260712-M3LC\stall\
  （招财进堡，AI 入景画品牌名+LOGO，客人认可）。
- 三视图成功案例（Chris 2026-08-12 确认，萃瑶早期批次）：
  - `D:\ComfyUI-backup\output\comfyui_zt_00742_.png`：干净单角色正面
    （无角/兽耳、玫瑰金礼服+皇冠、优雅）——正面参考；
  - `D:\ComfyUI-backup\output\comfyui_zt_00744_.png`：**单图三视图成功
    样例**（左=3/4侧、中=正面、右=背面，同一角色、金/玫瑰金长发、无角）——
    三视图合拼参考；
  - `D:\ComfyUI-backup\output\comfyui_zt_00754_.png`：单图三视角样例
    （左=正面、中=正面微侧、右=3/4侧面引导手势；粉色长发三态、玫瑰金鱼尾裙、
    无角）——与萃瑶粉发更接近，风格参考；
  - 教训：鹿角污染与特定提示词（「长发飘逸/侧面/背面」渲染鹿角状轮廓）及
    JIK0 鹿人 CHAR_BASE 有关；早期干净批次的提示词/种子值得复盘复用。

## 产品与架构

- Brand Brain 是 B2B AI 品牌顾问与 VI 手册自动生成平台，不是单次图片生成器。
- 生产网站运行在 Zeabur，共享项目和订单状态位于 Supabase。
- 本地 Windows Worker 轮询获得授权的任务，本地 ComfyUI 生成视觉素材，Worker 负责生成并上传 PPTX。
- 管理后台显示“已生成”不等于 Zeabur 完成了生成。诊断前必须确认任务实际经过哪条执行路径。
- 推送 `master` 可能自动触发生产部署；未经明确授权不得 push 或 merge 到 `master`。

## 动态生成与数据真源

- 修复平台生成器和数据契约，不直接修补某两个成品文件。
- 禁止为了通过样例或评审而写死客户品牌名、项目 ID、测试前后缀、颜色、Logo 元素、公仔名称、页码或行业物料。
- 正式品牌名、内部项目名称、项目 ID、行业、品牌色、Logo 语义、公仔购买意图和公仔素材完整度是不同数据概念；存在显式字段时不得互相猜测。
- `wantMascot`（或当前经代码确认的业务意图字段）决定客户是否选择 IP；资产存在不能证明客户购买了 IP。
- 公仔素材是否完成必须验证实际所需资产；布尔值、非空对象或 `!!mascotAssets` 不能绕过验证。
- 无 IP 手册必须做到：零公仔/IP 页面、零公仔/IP 文案。
- 物料必须来自可复用的行业规则。除非项目数据明确要求，面馆不得继承茶饮专属杯套等物料。
- 色板和 Logo 禁用规则必须来自项目真实色彩与已识别 Logo 元素；证据不足时使用安全的通用规则，不得套用其他品牌模板。

## 工作区纪律

- 每张任务开始时记录当前分支、HEAD、`origin/master` 和 `git status --short`。
- 保护无关改动和隔离分支内容；没有明确工单决定，不得跨分支复制或混合修改。
- 运行日志、生成的 PPTX/PDF/图片、临时测试产物和凭据不得进入 Git；只有经过评审且确实需要的测试 fixture 例外。
- 搜索优先使用 `rg` / `rg --files`，修改优先使用补丁式编辑。
- 保持原有 CRLF/LF 风格；diff 异常膨胀时，同时检查普通 diff 与 `--ignore-cr-at-eol`。
- 生成资产存储纪律（Chris 2026-08-12）：IP 公仔/生图成功后，存储目录只保留
  1-2 套成功资产（成熟版与 Q 版各留一套），失败产物全部删除、不备份；
  避免 ComfyUI output / logs 堆积失败尝试。

## 安全执行边界

- 未经当前任务授权，不得启动或停止 Worker、ComfyUI、本地服务器、定时 watcher 或其他进程。
- 未经授权，不得读写生产 Supabase、触发付费 AI、修改订单状态、上传成品、部署或执行客户可见操作。
- 离线测试必须清空外部凭据，且不能在本地失败后静默转用真实线上服务。
- 控制台与报告不得暴露客户数据和密钥值。

## 测试与评审

- 条件允许时，先用聚焦测试复现缺陷，再修改生产代码。生成了文件不等于语义和内容正确。
- VI 手册相关修改，应同时验证规划结果（Blueprint/pageId）和渲染后的 PPTX 文字/XML。
- 相关任务至少覆盖以下通用场景：
  - 无 IP 的餐饮/面食项目；
  - 素材完整的有 IP 饮品项目；
  - 已选择 IP 但素材不完整；
  - 名称中合法包含数字的品牌；
  - 两个不同色板项目，用于发现跨项目串色。
- 先跑最小相关测试。交接前根据改动运行语义回归、修改过的 `.mjs` 的 `node --check`，以及不产生增量文件的 TypeScript 检查。
- 不得用 `any`、`@ts-ignore` 或新增 `@ts-nocheck` 掩盖错误。已有 `@ts-nocheck` 是技术债，不代表类型安全；影响验证时必须报告。

## 完成标准

只有同时满足以下条件，任务才算完成：

1. 根因已在可复用的平台逻辑中修复。
2. 通用回归场景通过，且没有削弱断言。
3. 原有相关 PASS 没有退化。
4. diff 仅包含授权文件，没有行尾噪声。
5. 报告列出实际运行的测试、结果/退出码、剩余失败，以及是否发生 commit、push、部署、进程启停、远端写入或付费调用。

Chris 或产品负责人提供的任务 MD 定义当前目标，并可施加更严格的限制。除非遇到真实阻塞、缺少产品决定、权限、凭据、破坏性操作或必须扩大范围，否则应持续完成实现与验证，不要做一步停一步。

- 手册验收铁律（2026-08-13 教训）：**回执全绿 ≠ 成品合格**。VI 手册类工单验收必须拆包（pptx 解压）核对每页嵌入 media + 逐页双模型视觉核验（单公仔/核字/无乱码/无重复/无硬贴），回执须附逐页核验表；只信回归和 URL 即 PASS = 失职。手册工单附『硬性要求清单』防长对话丢要求。

- 防再发生框架（2026-08-13 根源定案后固化）：① 字段契约硬门——front/side/back/emotions/scenes 每字段图必须过『人数=1+姿态断言』才落库；② 视觉门升级——单公仔判定必须显式数人数并列出各姿态，多姿态/多角色即失败，双模型都过才算；③ 渲染-验收集一致性——回归断言渲染取用=验收通过 URL；④ 交付层拆包逐页核验表（人数/核字/无重复/无硬贴）为验收铁律；⑤ 页面图片重复/未知文字异常检测。任何『框架之外的结果』=硬门失败，不交付。

## 提示词合理性门与拦截记录（2026-08-21 主窗补充，Chris 批准）

- 提示词合理性门上线后，所有生成提示词必须过门：确定性规则（行业关键词 allow/deny、
  品牌名/结构/负面词）为第一档、语义复审（缓存优先）为第二档；拦截必有记录，落盘
  `logs/prompt-gate/blocked-*.json`（含时间/工单/行业族/规则 ID/提示词/修正 diff/费用）。
- 拦截后先自动修正提示词并**重跑同一道门**；修不过才 stall，绝不静默放行。
- 门/自动修正只改提示词，不改代码；「修管线」= 主窗依据拦截记录开工单，侧窗改
  代码/配方/数据，属独立人工动作。
- **定期核验（主窗职责）**：每个发布周期至少一次汇总 `logs/prompt-gate/blocked-*.json`，
  按 ruleId/行业族/场景角色统计；重复高频出现的拦截必须开出生产管线修复工单；
  连续两个周期同一 ruleId 仍高频出现且未开单 → 升级提醒 Chris。禁止「只拦不改」。
- 管理后台须展示拦截记录（列表/筛选/状态/费用，见对应工单）；主窗核验以真实记录为准，
  不只看后台摘要。

## 高峰/空闲时段排程（2026-08-21 Chris 批准）

- 高峰时段（北京时间）：9:00-12:00、14:00-18:00；空闲时段为其余时间。
- 空闲时段价格为高峰时段价格的一半（API/推理成本），**优先在空闲时段开工**。
- 高峰时段：不开新工单、不启动重型生成/推理任务（ComfyUI/DeepSeek 等）；已在运行的
  工单继续跑完、不中断。
- 主窗在高峰时段提醒 Chris；确需高峰开工须 Chris 明确批准。
- 生效日期：2026-08-21 起。

## RTK 使用纪律（2026-08-14 主窗补充，Chris 批准）

- 本机已装 RTK（v0.45.0）：`D:\disk\_tools\rtk\rtk.exe`，PATH 副本 `C:\Users\Administrator\.local\bin\rtk.exe`。
- 高输出量命令优先加 `rtk` 前缀（可省 60-90% 命令输出 token）：`rtk git status`、`rtk git diff`、`rtk git log`、`rtk ls`、`rtk find`、`rtk grep`、`rtk node --check`、测试类命令等。
- 关键命令必须原样输出、不得过滤：`node ... signal-bridge.mjs ...`、JSON/哈希/清单类输出 → 用 `rtk proxy <cmd>` 或不用 rtk。
- 未知命令 rtk 会原样透传，可放心前缀；内建命令（cd/echo/set）不要加 rtk；复合命令只包目标可执行，如 `cd x && rtk git status`。
- 效果以 `rtk gain` 为准；若效率/质量退化，运行 `D:\360MoveData\Users\Administrator\Desktop\汇报\CODEX救回\GOBACKCODEX.bat` 一键还原。
